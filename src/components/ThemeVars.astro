---
import { site } from "../config/site"

const primary = site.primaryColor ?? "#0b5fff"
const accent = site.accentColor ?? "#00c389"
---
<style is:global>
  :root {
    --color-primary: {primary};
    --color-accent: {accent};
  }
</style>