---
import ButtonFreeAssessment from './ButtonFreeAssessment.astro';
import ButtonPhoneNumberOutline from './ButtonPhoneNumberOutline.astro';
interface Props {
  eyebrow?: string;
  headline: string;
  subhead: string;
  primaryText?: string;
  primaryHref?: string;
}

const { 
  eyebrow,
  headline,
  subhead,
  primaryText,
  primaryHref,
} = Astro.props;
---

<section 
  class="relative w-full bg-cover bg-center py-20 pt-25 md:py-30 md:pt-40"
  style="background-image: url('https://RebelApeCDN.b-cdn.net/CTI-Roofing/hero-roofing-crew.webp')"
>
  <!-- Dark overlay for text contrast -->
  <div class="absolute inset-0 bg-black/60"></div>
  
  <div class="relative max-w-6xl mx-auto px-6 text-center text-white">
    {eyebrow && (
      <p class="text-sm md:text-base text-green-400 font-semibold mb-4 uppercase tracking-wider">
        {eyebrow}
      </p>
    )}
    
    <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight">
      Think Something's Wrong With Your Roof?
    </h1>
    
    <h2 class="text-2xl md:text-3xl font-semibold mb-6">
      Get Answers From Northern Utah's Trusted Roofing Experts
    </h2>

    <p class="text-lg md:text-xl mb-10 max-w-3xl mx-auto leading-relaxed">
      Don't wait until a small issue turns into a costly repair. Our experienced roofing professionals provide thorough inspections and honest assessments to keep your home safe and secure. Trust us to deliver quality service with no pressure and no obligation.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col md:flex-row gap-4 justify-center items-center mb-6">
      <ButtonFreeAssessment />
      <ButtonPhoneNumberOutline />
    </div>

    <p class="text-sm text-gray-200 max-w-xl mx-auto">
      ✓ Free assessment  |  ✓ No obligation  |  ✓ We answer within 30 seconds or call you back
    </p>
  </div>
</section>